# Built binary at the repo root only — anchored with a leading slash so it does
# NOT also match the cmd/checkfleet/ package directory.
/checkfleet
dist/
.DS_Store

# Jekyll docs site (built by .github/workflows/pages.yml).
docs/_site/
docs/.jekyll-cache/
docs/Gemfile.lock
